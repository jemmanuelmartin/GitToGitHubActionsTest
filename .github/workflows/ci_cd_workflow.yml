l# .github/workflows/nodejs-build-deploy.ymlname: Node.js Build and Deployon:  push:    branches:      - main  pull_request:    branches:      - mainjobs:  build:    runs-on: ubuntu-latest    steps:      - name: Checkout Code        uses: actions/checkout@v3      - name: Setup Node.js        uses: actions/setup-node@v3        with:          node-version: '16' # Specify the Node.js version you want to use      - name: Install Dependencies        run: npm install      - name: Run Tests        run: npm test      - name: Build Application        run: npm run build  deploy:    runs-on: ubuntu-latest    needs: build    steps:      - name: Checkout Code        uses: actions/checkout@v3      - name: Setup Node.js        uses: actions/setup-node@v3        with:          node-version: '16' # Specify the Node.js version you want to use      - name: Install Dependencies        run: npm install      - name: Deploy Application        env:          NODE_ENV: production        run: |          echo "Deploying application..."          # Replace the below command with your actual deployment script or command          npm run deploy